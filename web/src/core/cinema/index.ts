/**
 * Точка входа кинозала.
 *
 * Раньше всё лежало одним файлом `cinema.ts`; здесь — реестр площадок, типы каталога, клиент
 * к службе и форматирование чисел раздельно, каждое своим файлом. Старые импорты
 * `from '../core/cinema'` продолжают работать как есть: резолвер модулей понимает директорию
 * с `index.ts` так же, как раньше понимал файл `cinema.ts`, и переписывать пути по всему
 * коду ради самого разделения не нужно.
 */
export * from './types';
export * from './format';
export * from './providers';
export * from './api';
